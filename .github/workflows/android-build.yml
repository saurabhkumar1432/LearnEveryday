name: Android CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle
    
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
    
    - name: Install Android SDK packages
      run: |
        set -e
        sdkmanager "platform-tools" "platforms;android-34" "build-tools;34.0.0" || \
        "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager" "platform-tools" "platforms;android-34" "build-tools;34.0.0"
        
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      
    - name: Build with Gradle
      run: ./gradlew build --stacktrace
      
    - name: Build Release APK
      run: ./gradlew assembleRelease --stacktrace
      
    - name: Decode Keystore
      run: |
        echo "${{ secrets.SIGNING_KEY }}" | base64 -d > keystore.jks
      
    - name: Align APK
      run: |
        $ANDROID_SDK_ROOT/build-tools/34.0.0/zipalign -v -p 4 \
          app/build/outputs/apk/release/app-release-unsigned.apk \
          app/build/outputs/apk/release/app-release-unsigned-aligned.apk
      
    - name: Sign APK
      run: |
        $ANDROID_SDK_ROOT/build-tools/34.0.0/apksigner sign \
          --ks keystore.jks \
          --ks-key-alias "${{ secrets.ALIAS }}" \
          --ks-pass pass:"${{ secrets.KEY_STORE_PASSWORD }}" \
          --key-pass pass:"${{ secrets.KEY_PASSWORD }}" \
          --out app/build/outputs/apk/release/LearnEveryday-signed.apk \
          app/build/outputs/apk/release/app-release-unsigned-aligned.apk
      
    - name: Verify APK Signature
      run: |
        $ANDROID_SDK_ROOT/build-tools/34.0.0/apksigner verify -v \
          app/build/outputs/apk/release/LearnEveryday-signed.apk
      
    - name: Get APK info
      run: |
        ls -lh app/build/outputs/apk/release/
        $ANDROID_SDK_ROOT/build-tools/34.0.0/aapt dump badging app/build/outputs/apk/release/LearnEveryday-signed.apk | grep -E 'package|versionCode|versionName|sdkVersion|targetSdkVersion'
      
    - name: Cleanup sensitive files
      if: always()
      run: |
        rm -f keystore.jks
      
    - name: Upload Signed APK as artifact
      uses: actions/upload-artifact@v4
      with:
        name: LearnEveryday-Release-APK
        path: app/build/outputs/apk/release/LearnEveryday-signed.apk
        retention-days: 30
        
    - name: Get version name
      id: version
      run: |
        VERSION=$(grep "versionName" app/build.gradle.kts | awk '{print $3}' | tr -d '"')
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        
    - name: Create Release
      if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      uses: softprops/action-gh-release@v2
      with:
        tag_name: v${{ steps.version.outputs.version }}-${{ github.run_number }}
        name: Release v${{ steps.version.outputs.version }} Build ${{ github.run_number }}
        body: |
          ## 🎓 LearnEveryday v2.1.0 - Minor Release
          
          ### ✨ What's New in v2.1.0
          
          #### � Refresh Topic Suggestions
          - New refresh button to get fresh AI-generated topic suggestions
          - Topics persist across app restarts until manually refreshed
          - Spinning animation feedback while loading new topics
          
          #### 🎨 Dark Mode Support
          - New theme selector in Settings (System/Light/Dark)
          - Theme preference persists across app restarts
          - Seamless theme switching without app restart
          
          #### 📤 Export Your Data
          - Export all learning plans and progress as JSON
          - Share exported data via any app
          - Includes curriculum details, lessons, and progress
          
          #### 🎯 Improved Topic Selection Flow
          - Tapping a suggested topic now opens the Create Plan dialog
          - Topic name pre-filled for quick customization
          - User can adjust difficulty and lesson count before creating
          
          #### 🔧 UI/UX Improvements
          - Fixed back button icons (now correctly pointing left)
          - Rounded corners on all dialog boxes
          - Fixed settings toolbar - now stays at top while scrolling
          - Search button text no longer cut off
          - Consistent terminology throughout the app
          
          #### 🤖 Updated AI Models
          - Latest model names for all providers (Nov 2025)
          - Gemini: gemini-2.0-flash, gemini-2.5-flash/pro
          - Claude: claude-sonnet-4.5, claude-opus-4.5
          - OpenAI: gpt-4o, gpt-4.1-mini
          - OpenRouter: Free tier models updated
          
          ### 🛠️ Technical Details
          - **Version**: 2.1.0 (versionCode: 3)
          - **Target SDK**: 34 (Android 14)
          - **Min SDK**: 24 (Android 7.0)
          
          ### 📦 Installation
          1. Download the APK file below
          2. Enable "Install from Unknown Sources" on your Android device
          3. Install the APK
          
          ---
          📝 Commit: ${{ github.sha }}
          
        files: |
          app/build/outputs/apk/release/LearnEveryday-signed.apk
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
