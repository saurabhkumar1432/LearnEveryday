<?xml version="1.0" encoding="utf-8"?>
<!-- Learning Plan Card Item -->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/spacing_md_plus"
    app:cardCornerRadius="@dimen/corner_radius_xl"
    app:cardElevation="@dimen/elevation_md"
    app:strokeWidth="1dp"
    app:strokeColor="@color/divider"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Gradient Accent Bar -->
        <View
            android:id="@+id/accentBar"
            android:layout_width="6dp"
            android:layout_height="0dp"
            android:background="@drawable/bg_gradient_primary"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Content Container -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/card_content_padding"
            android:layout_marginStart="@dimen/spacing_sm"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@id/accentBar"
            app:layout_constraintEnd_toEndOf="parent">

            <!-- Header Row -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <!-- Category Icon -->
                <com.google.android.material.card.MaterialCardView
                    android:layout_width="@dimen/card_icon_size"
                    android:layout_height="@dimen/card_icon_size"
                    app:cardCornerRadius="@dimen/corner_radius_md"
                    app:cardElevation="0dp"
                    app:strokeWidth="0dp"
                    app:cardBackgroundColor="@color/primary_container">

                    <ImageView
                        android:id="@+id/iconCategory"
                        android:layout_width="@dimen/icon_size_md"
                        android:layout_height="@dimen/icon_size_md"
                        android:layout_gravity="center"
                        android:src="@drawable/ic_book"
                        app:tint="@color/primary" />

                </com.google.android.material.card.MaterialCardView>

                <!-- Title and Metadata -->
                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical"
                    android:layout_marginStart="@dimen/spacing_md">

                    <TextView
                        android:id="@+id/tvTitle"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:text="Python Programming"
                        android:textSize="@dimen/text_size_title_md"
                        android:textStyle="bold"
                        android:textColor="@color/text_primary"
                        android:maxLines="2"
                        android:ellipsize="end"
                        tools:text="Python Programming for Beginners" />

                    <!-- Difficulty Chip -->
                    <com.google.android.material.chip.Chip
                        android:id="@+id/chipDifficulty"
                        style="@style/Widget.Material3.Chip.Assist"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Beginner"
                        android:textSize="11sp"
                        app:chipMinHeight="24dp"
                        app:chipCornerRadius="12dp"
                        android:layout_marginTop="@dimen/spacing_xs" />

                </LinearLayout>

                <!-- Menu Button -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnMenu"
                    style="@style/Widget.Material3.Button.IconButton"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    app:icon="@drawable/ic_more"
                    app:iconTint="@color/text_tertiary" />

            </LinearLayout>

            <!-- Description -->
            <TextView
                android:id="@+id/tvDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Learn Python from scratch with practical examples"
                android:textSize="@dimen/text_size_body_md"
                android:textColor="@color/text_secondary"
                android:maxLines="2"
                android:ellipsize="end"
                android:layout_marginTop="@dimen/spacing_sm_plus"
                tools:text="Master the fundamentals of Python programming with hands-on projects" />

            <!-- Progress Section -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginTop="@dimen/spacing_md_plus">

                <!-- Progress Bar -->
                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical">

                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal"
                        android:gravity="center_vertical"
                        android:layout_marginBottom="@dimen/spacing_xs">

                        <TextView
                            android:id="@+id/tvProgress"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="45%"
                            android:textSize="@dimen/text_size_caption"
                            android:textStyle="bold"
                            android:textColor="@color/primary" />

                        <TextView
                            android:id="@+id/tvLessons"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text=" • 9 of 20 lessons"
                            android:textSize="@dimen/text_size_caption"
                            android:textColor="@color/text_tertiary" />

                    </LinearLayout>

                    <com.google.android.material.progressindicator.LinearProgressIndicator
                        android:id="@+id/progressBar"
                        android:layout_width="match_parent"
                        android:layout_height="@dimen/progress_bar_height"
                        android:progress="45"
                        android:max="100"
                        app:trackThickness="@dimen/progress_bar_height"
                        app:trackCornerRadius="3dp"
                        app:indicatorColor="@color/primary"
                        app:trackColor="@color/surface_variant" />

                </LinearLayout>

                <!-- Action Button -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnContinue"
                    style="@style/Widget.Material3.Button.TonalButton"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Continue"
                    android:textSize="@dimen/text_size_caption"
                    android:paddingStart="@dimen/spacing_md"
                    android:paddingEnd="@dimen/spacing_md"
                    android:layout_marginStart="@dimen/spacing_md"
                    app:icon="@drawable/ic_play"
                    app:iconSize="16dp" />

            </LinearLayout>

            <!-- Metadata Footer -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginTop="@dimen/spacing_sm_plus"
                android:paddingTop="@dimen/spacing_sm"
                android:background="?attr/selectableItemBackground">

                <!-- Duration -->
                <ImageView
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:src="@drawable/ic_schedule"
                    app:tint="@color/text_tertiary" />

                <TextView
                    android:id="@+id/tvDuration"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="2 weeks"
                    android:textSize="@dimen/text_size_caption"
                    android:textColor="@color/text_tertiary"
                    android:layout_marginStart="@dimen/spacing_xs" />

                <!-- Separator -->
                <View
                    android:layout_width="4dp"
                    android:layout_height="4dp"
                    android:layout_marginStart="@dimen/spacing_sm_plus"
                    android:layout_marginEnd="@dimen/spacing_sm_plus"
                    android:background="@drawable/bg_dot"
                    android:backgroundTint="@color/text_tertiary" />

                <!-- Updated Date -->
                <ImageView
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:src="@drawable/ic_calendar"
                    app:tint="@color/text_tertiary" />

                <TextView
                    android:id="@+id/tvUpdated"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Updated 2 days ago"
                    android:textSize="@dimen/text_size_caption"
                    android:textColor="@color/text_tertiary"
                    android:layout_marginStart="@dimen/spacing_xs" />

            </LinearLayout>

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>
